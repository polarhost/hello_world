from setuptools import setup, find_packages

setup(
    name="hello",
    version="0.1.0",
    packages=find_packages(),
    description="hello world package",
    author="polarhost",
    python_requires=">=3.6",
)